<!DOCTYPE html>

<title>Edit Compte</title>

{% block body %}
    <h1>Edit Compte</h1>

    {{ include('compte/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('All_comptes') }}">back to list</a>

    {{ include('compte/_delete_form.html.twig') }}
{% endblock %}
