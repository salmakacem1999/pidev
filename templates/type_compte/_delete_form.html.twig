

<form method="post" action="{{ path('typeCompte_delete', {'id': type_compte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_compte.id) }}">
    <button  class="btn btn-danger w-100 "> Delete</button>
</form> 


