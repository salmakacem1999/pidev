<form method="post" action="{{ path('app_carte_bancaire_delete', {'id': carte_bancaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carte_bancaire.id) }}">
    <button class="btn btn-primary ">Delete</button>
</form>
